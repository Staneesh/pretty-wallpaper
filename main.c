#include <stdio.h>
#include <stdint.h>

//NOTE(stanisz): requires stdio.h to be included before this line
#include "utils.c"

int main(i32 argc, char** argv)
{
	UNUSED(argc);
	UNUSED(argv);

	return 0;
}	
